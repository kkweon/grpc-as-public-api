apiVersion: v1
kind: Service
metadata:
  labels:
    app: envoy
  name: envoy
spec:
  selector:
    app: envoy
  type: ClusterIP
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: http
